{{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
    
    <div class="card-body">

        <div class="mb-3">
            {{ form_row(form.sujet, {'label': 'sujet', 'attr': {'class': 'form-control', 'placeholder': 'sujet'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.description, {'label': 'Description', 'attr': {'class': 'form-control', 'placeholder': 'description'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.idQuestion, {'label': 'Question', 'attr': {'class': 'form-control', 'placeholder': 'Question'}}) }}
        </div>

        <div class="text-center">
            <button type="submit" class="btn bg-gradient-info w-100 mt-4 mb-0">{{ button_label|default('Save') }}</button>
        </div>
    </div>
{{ form_end(form) }}