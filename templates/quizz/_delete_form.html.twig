<form method="post" action="{{ path('app_quizz_delete', {'idQuizz': quizz.idQuizz}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quizz.idQuizz) }}">
    <button class="btn">Delete</button>
</form>
