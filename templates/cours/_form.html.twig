{{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
    
    <div class="card-body">

        <div class="mb-3">
            {{ form_row(form.title, {'label': 'Title', 'attr': {'class': 'form-control', 'placeholder': 'title'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.contenu, {'label': 'Conetnt', 'attr': {'class': 'form-control', 'placeholder': 'contenu'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.etat, {'label': 'State', 'attr': {'class': 'form-control', 'placeholder': 'etat'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.idCat, {'label': 'Category', 'attr': {'class': 'form-control', 'placeholder': 'idCat'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.rate, {'label': 'Rate', 'attr': {'class': 'form-control', 'placeholder': 'rate'}}) }}
        </div>


        <div class="text-center">
            <button type="submit" class="btn bg-gradient-info w-100 mt-4 mb-0">{{ button_label|default('Save') }}</button>
        </div>
    </div>
{{ form_end(form) }}