{% extends 'base.html.twig' %}

{% block title %}ReponseRec{% endblock %}

{% block body %}
    <h1>ReponseRec</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse_rec.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ reponse_rec.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reponse_rec.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reponse_rec.date ? reponse_rec.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_rec_index') }}">back to list</a>

    <a href="{{ path('app_reponse_rec_edit', {'id': reponse_rec.id}) }}">edit</a>

    {{ include('reponse_rec/_delete_form.html.twig') }}
{% endblock %}
